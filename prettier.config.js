/**
 * `prettier-plugin-tailwindcss` is not installed, so a `tailwindConfig` key
 * here is only an unknown option Prettier warns about on every run. Class
 * ordering is enforced by eslint-plugin-tailwindcss instead.
 */
module.exports = {};
